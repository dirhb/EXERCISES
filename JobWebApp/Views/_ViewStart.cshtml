@{
    // This runs before every view
    // It tells every .cshtml page to use _Layout.cshtml automatically
    // Without this, you'd have to write the full HTML in every single view
    Layout = "_Layout";
}
